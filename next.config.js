// Default Pages configuration for Next
const nextConfig = {
  images: {unoptimized: true},
  basePath: "/krystal-cloud-demo",
  output: "export",
  eslint: {
    ignoreDuringBuilds: true,
  },
}
module.exports = nextConfig
